/*
  bug.n -- tiling window management

  Single source of truth for the version string. Consumed by Main.ahk and
  Bench_main.ahk via #Include so a release bump touches only this file.
*/

VERSION := "9.4.1"
